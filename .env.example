# Required by api/chat.js. Set this in your Vercel project's Environment
# Variables (Project Settings -> Environment Variables) — never commit a
# real key to this file or anywhere in the repo. Get a key at
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
